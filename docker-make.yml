---
version: '1'
name: metalctl
username: metal
registry-host: registry.fi-ts.io
default-build-args:
  - http_proxy=${HTTP_PROXY}
  - https_proxy=${HTTP_PROXY}
  - HTTP_PROXY=${HTTP_PROXY}
  - HTTPS_PROXY=${HTTP_PROXY}
  - NO_PROXY=${NO_PROXY}
  - no_proxy=${NO_PROXY}
builds:
  - name: metalctl-slug
    tags:
      - ${CI_COMMIT_REF_NAME}
  - name: metalctl-master
    tags:
      - latest
